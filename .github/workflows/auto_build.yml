name: Notify Main Repository

on:
  repository_dispatch:
    types: [Auto_Build]  # trigger from submodules repositories

jobs:
  notify:
    runs-on: Main_Runner  # This label added to the "HP-ZBPG8-TEST2" machine for TWI_ORG_RUNNER runner
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.YOUSSEF_RUNNER }}
    
      - name: Trigger update-submodules workflow
        run: |
          echo main
          echo data
          echo data
          echo ${{ github.event.client_payload.sha_num }}
          echo ${{ github.event.client_payload.branch_name }}
